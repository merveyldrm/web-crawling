# Gerçek Zamanlı RAG Sistemi Bağımlılıkları

# Temel kütüphaneler
numpy>=1.21.0
scikit-learn>=1.0.0
pandas>=1.3.0

# Embedding ve NLP
sentence-transformers>=2.2.0
transformers>=4.20.0
torch>=1.12.0

# RAG ve AI entegrasyonu  
openai>=0.27.0  # OpenAI API (opsiyonel)
langchain>=0.0.200  # LangChain framework (gelişmiş RAG için)

# Veritabanı
sqlite3  # Python built-in

# Web scraping (harici kaynak toplama için)
requests>=2.28.0
beautifulsoup4>=4.11.0
selenium>=4.0.0

# Async ve zamanlama
asyncio  # Python built-in
schedule>=1.2.0
threading  # Python built-in

# Monitoring ve loglama
watchdog>=2.1.0  # File system monitoring
logging  # Python built-in

# API ve web servisleri (gelişmiş entegrasyon için)
fastapi>=0.85.0  # Web API oluşturmak için
uvicorn>=0.18.0  # ASGI server
websockets>=10.0  # Real-time updates için

# Notification sistemi (opsiyonel)
slack-sdk>=3.19.0  # Slack bildirimleri
smtplib  # Email (Python built-in)

# Data processing
python-dateutil>=2.8.0
hashlib  # Python built-in
json  # Python built-in
pathlib  # Python built-in

# Development ve testing
pytest>=7.0.0
pytest-asyncio>=0.20.0 